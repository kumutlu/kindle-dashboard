# PW1 native mxc_rtc PR context

This branch is intentionally isolated from `main` while the native PW1 scheduler is implemented and verified. The implementation replaces KindleCron only for the PW1 path and keeps other device schedulers unchanged.
